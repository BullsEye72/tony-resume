import { chakraPetch } from "@/app/fonts";

export default function Separator() {
  return (
    <div className={`lgc-blue-bg pl-8 w-full h-16 ${chakraPetch.className} text-3xl flex items-center pl-2 uppercase`}>
      Developpeur Fullstack
    </div>
  );
}
